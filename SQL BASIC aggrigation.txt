1
SELECT `productVendor` , COUNT(*) AS or_der 
FROM `products`
GROUP BY `productVendor`
2
SELECT `status` , COUNT(*) 
FROM `orders`
GROUP BY status
3 
SELECT `salesRepEmployeeNumber`,AVG(`creditLimit`)
FROM `customers`
GROUP BY `salesRepEmployeeNumber`
4
SELECT country, MAX(`creditLimit`),MIN(`creditLimit`)
FROM `customers`
GROUP BY country
5
SELECT `officeCode`,COUNT(`employeeNumber`)
FROM `employees`
GROUP BY officeCode
6
SELECT `customerNumber` ,AVG(`amount`) AS av
FROM `payments` 
GROUP BY `customerNumber`
ORDER BY av DESC
7
SELECT `productVendor`,AVG(`buyPrice`)
FROM `products` 
GROUP BY `productVendor`
8
SELECT `salesRepEmployeeNumber` ,COUNT(`customerNumber`)
FROM `customers` 
GROUP BY `salesRepEmployeeNumber`
9
SELECT `orderDate` ,COUNT(*) AS sum
FROM `orders`
GROUP BY `orderDate`
ORDER BY sum DESC
10 
SELECT `orderDate` ,COUNT(*) AS sum 
FROM `orders`
GROUP BY `orderDate`
ORDER BY sum DESC
11
SELECT `productScale` ,COUNT(*)
FROM `products` 
GROUP BY `productScale`